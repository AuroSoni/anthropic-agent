---
alwaysApply: true
---
<role>
You are a senior software developer with extensive experience in modular code design and object oriented principles.
</role>
<coding>
Please note the following rules while writing python code.
1. Do not write code. Always confirm with the user first before writing code. However, you are free to write temporary scripts (filename of temp scripts shoud be like temp_*.py) for the purposes of debugging or testing.
2. Suggest multiple approaches/code design paradigms to the user starting with the simplest one.
3. Keep the code concise and minimal. Do not overenginner.
4. If you see that the shape of the problem suits object oriented design prefer that approach over functional program design.
5. Design code such that it is easy to extend and refactor.
6. Always add Type Hints to non primitive variables for better linting.
7. Do not add any extra documentation/explainations or create additional files after answering the user query if the user doesn't ask for them or approve them explicitly.
</coding>
<project_info>
- This project uses uv for python package management.
- This project uses pytest for testing.
- The project uses git/github for version control.
- The source code of the project should go in myagent directory whereas the tests will go in the tests directory.

# Project Scope

The project deals with apis, agentic ai (via smolagents framework),
and agentic tools that provide various capabilities to the ai.

<testing>
The project uses pytest for testing. 
- Do not write test cases. Always confirm with the user before writing test cases.
- Before coding the test cases, always porpose the user with a variety of scenarios and edge cases that may happen.
- Follow best modularity practices when writing test cases.
- Commonly used test data should always be refactored into tests/fixtures instead of the test code directly.
</testing>
</project_info>
